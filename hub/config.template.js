// Генерируется при старте контейнера из переменных окружения (.env).
// Пустые значения игнорируются — тогда хаб использует фолбэк из index.html.
window.LESSON_URLS = {
  1: "${LAB1_URL}",
  2: "${LAB2_URL}",
  3: "${LAB3_URL}",
  4: "${LAB4_URL}",
  5: "${LAB5_URL}"
};
